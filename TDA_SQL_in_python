## implementing SQL in Python: sqlite3

!pip install sqlite3

import sqlite3

conn = sqlite3.connect('database.db')


cursor = conn.cursor()

cursor.execute("CREATE TABLE students (name TEXT, grade REAL)")

cursor.execute("INSERT INTO students VALUES ('John', 4)")

cursor.execute("SELECT * FROM students")

all_students = cursor.fetchall()

all_students

cursor.executemany("INSERT INTO students (name, grade) VALUES (?, ?)", students_list)

all_students = cursor.fetchall()

print(all_students)
